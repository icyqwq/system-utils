{
  "name": "system-utils",
  "version": "1.0.0",
  "description": "Cross-platform native system utilities module",
  "main": "index.js",
  "types": "index.d.ts",
  "gypfile": true,
  "scripts": {
    "install": "node-gyp rebuild",
    "build": "node-gyp rebuild",
    "rebuild:electron": "npx @electron/rebuild -f -w system-utils"
  },
  "dependencies": {
    "node-addon-api": "^7.0.0"
  },
  "devDependencies": {
    "node-gyp": "^10.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
